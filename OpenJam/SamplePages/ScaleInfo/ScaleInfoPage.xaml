<Page
    x:Class="OpenJam.SamplePages.ScaleInfo.ScaleInfoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls" 
    xmlns:core="using:OpenJam.Music.Core"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="ScalesTemplate" x:DataType="core:Scale" >
            <StackPanel>
                <TextBlock Text="{x:Bind Name}"/>
                <TextBlock Text="{x:Bind Details}"/>
                <TextBlock Text="{x:Bind PitchesDetails}"/>
            </StackPanel>
        </DataTemplate>
    </Page.Resources>
    
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <controls:AdaptiveGridView 
            ItemsSource="{x:Bind ViewModel.Scales}"
            OneRowModeEnabled="False"
            ItemHeight="200"
            DesiredWidth="300"
            SelectionMode="None"
            IsItemClickEnabled="False"
            ItemTemplate="{StaticResource ScalesTemplate}"/>
    </Grid>
</Page>
