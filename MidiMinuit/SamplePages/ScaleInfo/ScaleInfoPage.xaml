<Page
    x:Class="MidiMinuit.SamplePages.ScaleInfo.ScaleInfoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MidiMinuit"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:scales="using:MidiMinuit.Music.Core.Scales"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="ScalesTemplate" x:DataType="scales:Scale" >
            <StackPanel>
                <TextBlock Text="{x:Bind Name}"/>
                <TextBlock Text="{x:Bind Details}"/>
                <TextBlock Text="{x:Bind NoteDetails}"/>
            </StackPanel>
        </DataTemplate>
    </Page.Resources>
    
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <controls:AdaptiveGridView 
            ItemsSource="{x:Bind ViewModel.Scales}"
            OneRowModeEnabled="False"
            ItemHeight="200"
            DesiredWidth="300"
            SelectionMode="None"
            IsItemClickEnabled="False"
            ItemTemplate="{StaticResource ScalesTemplate}"/>
    </Grid>
</Page>
